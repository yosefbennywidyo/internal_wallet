<div class="col">
  <h1>Stocks</h1>

  <div id="stocks">
    <% @stocks.each do |stock| %>
      <%= render stock %>
      <p>
        <%= link_to "Show this stock", "stocks/#{stock.code}" %>
      </p>
    <% end %>
  </div>
  <%= link_to "New stock", new_stock_path %>
</div>