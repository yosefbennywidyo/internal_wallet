<div class="col">
  <h1>Teams</h1>

  <div id="teams">
    <% @teams.each do |team| %>
      <%= render team %>
      <p>
        <%= link_to "Show this team", "teams/#{team.code}" %>
      </p>
    <% end %>
  </div>
  <%= link_to "New team", new_team_path %>
</div>