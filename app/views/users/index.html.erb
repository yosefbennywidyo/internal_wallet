<div class="col">
  <h1>Users</h1>

  <div id="users">
    <% @users.each do |user| %>
      <%= render user %>
      <p>
        <%= link_to "Show this user", "users/#{user.code}" %>
      </p>
    <% end %>
  </div>
  <%= link_to "New user", new_user_path %>
</div>